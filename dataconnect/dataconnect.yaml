specVersion: "v1beta"
serviceId: "diak"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "diak-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
